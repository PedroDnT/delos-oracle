#!/bin/bash
echo "🔨 Running quick build check..."
# For Hardhat/Solidity projects
npx hardhat compile --quiet 2>/dev/null
if [ $? -eq 0 ]; then
  echo "✅ Build check passed."
else
  echo "❌ Build failed. Please check your syntax."
fi
exit 0 # We keep it 0 so it doesn't block Claude, just warns you
