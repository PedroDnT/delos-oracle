---
phase: 01-anchor-program
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: true
requirements:
  - NF2.1
  - NF3.1

must_haves:
  truths:
    - "All 6 Anchor tests pass on localnet"
    - "initialize test: oracle PDA created, authority set to deployer, lastUpdated > 0"
    - "updateRate test: SELIC rate type 0 stored with correct value and date"
    - "invalidRateType test: rate_type=99 rejected with InvalidRateType error"
    - "batchUpdateRates test: all 4 daily rates updated atomically"
    - "updateFocus test: all 4 Focus expectations stored with correct year and referenceDate"
    - "authorityEnforcement test: intruder signer rejected with Unauthorized error"
    - "transferAuthority test: authority transferred and then restored"
    - "No authority bypass exists — Unauthorized error confirmed by test"
  artifacts:
    - path: "target/idl/delos_oracle.json"
      provides: "IDL loaded by test suite"
      contains: "initialize"
    - path: "target/types/delos_oracle.ts"
      provides: "TypeScript types used by tests"
      exports: ["DelosOracle"]
    - path: "tests/anchor/delos-oracle.ts"
      provides: "Test suite — 6 it() blocks"
      contains: "rejects update from unauthorized signer"
  key_links:
    - from: "tests/anchor/delos-oracle.ts"
      to: "target/types/delos_oracle.ts"
      via: "TypeScript import at line 4"
      pattern: "DelosOracle.*target/types/delos_oracle"
    - from: "tests/anchor/delos-oracle.ts"
      to: "programs/delos-oracle/src/lib.rs"
      via: "anchor test deploys and calls program instructions"
      pattern: "anchor test"
---

<objective>
Run `anchor test` against the localnet to verify all 6 test cases pass. This confirms the program logic is correct — authority enforcement, rate storage, batch updates, Focus updates, and key rotation all work as implemented.

Purpose: Tests provide the verifiable evidence that the program correctly enforces authorization (no bypass possible) and stores all 8 data fields correctly. This is required for grant credibility (NF3.1) and security (NF2.1).

Output: All 6 passing tests. `anchor test` exits 0.
</objective>

<execution_context>
@/Users/pedrotodescan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedrotodescan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-anchor-program/01-CONTEXT.md
@.planning/phases/01-anchor-program/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Types exported by Plan 01 build output — executor uses these directly -->

From target/types/delos_oracle.ts (generated by anchor build):
- Exports `DelosOracle` interface — used by test at line 4:
  `import { DelosOracle } from "../../target/types/delos_oracle";`

Test file structure (tests/anchor/delos-oracle.ts):
- 6 `it()` blocks: initialize, updateRate(SELIC), rejectInvalidRateType, batchUpdateRates, updateFocus, rejectUnauthorized, transferAuthority
- Uses PDA seeds: `[Buffer.from("oracle")]`
- Authority = provider.wallet (localnet keypair at ~/.config/solana/id.json)
- Program ID (localnet): `CUagkkUBPL8WarfaV1ceZ6mkVppxq2JsanZa111Dzikp` (from Anchor.toml [programs.localnet])
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and run anchor test</name>
  <read_first>
    - tests/anchor/delos-oracle.ts (test suite being run — understand what each test asserts)
    - Anchor.toml (test script: "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts")
    - tsconfig.json (TypeScript configuration)
    - package.json (devDependencies — @coral-xyz/anchor, chai, ts-mocha)
  </read_first>
  <files>
  </files>
  <action>
    First, ensure Node.js dependencies are installed:

    ```
    yarn install
    ```

    Then run the full test suite:

    ```
    anchor test
    ```

    `anchor test` will:
    1. Start a local validator (localnet)
    2. Build and deploy the program (using localnet program ID from Anchor.toml)
    3. Run ts-mocha on tests/anchor/delos-oracle.ts
    4. Shut down the validator

    Expected output: 6 passing tests with names:
    - "initializes the oracle account"
    - "updates SELIC rate (rate_type=0)"
    - "rejects invalid rate_type"
    - "batch updates all 4 daily rates atomically"
    - "updates Focus Boletim expectations"
    - "rejects update from unauthorized signer"
    - "transfers authority to a new key"

    Wait — the test file has 7 `it()` blocks. Count them: initialize, updateSELIC, rejectInvalidRateType, batchUpdate, updateFocus, rejectUnauthorized, transferAuthority = 7 tests.

    If tests fail, diagnose before modifying anything:
    - TypeScript errors about missing types: IDL generation may have failed in Plan 01 — re-run `anchor build` first
    - "Account does not exist" on initialize: localnet validator may not have started — check `anchor test` output for validator errors
    - Airdrop failures in authority test: localnet airdrop works locally, this should not fail
    - "Cannot find module '../../target/types/delos_oracle'": run `anchor build` first (Plan 01 prerequisite)

    Do NOT modify test files or program source. Tests are pre-written and must pass as-is.
  </action>
  <verify>
    <automated>anchor test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `anchor test` exits with code 0
    - Output contains "passing" with count >= 6: grep "passing" in anchor test output
    - Output contains "0 failing": grep "0 failing" or no "failing" line in output
    - Output includes "initializes the oracle account" as a passing test
    - Output includes "rejects update from unauthorized signer" as a passing test (authority enforcement confirmed)
    - Output includes "transfers authority to a new key" as a passing test
    - No "Error" or "AssertionError" lines present in test output
  </acceptance_criteria>
  <done>anchor test exits 0. All tests report passing. The "rejects update from unauthorized signer" test confirms no authority bypass is possible.</done>
</task>

</tasks>

<verification>
```
anchor test 2>&1 | tee /tmp/anchor-test-output.txt
grep -E "passing|failing" /tmp/anchor-test-output.txt
grep "rejects update from unauthorized signer" /tmp/anchor-test-output.txt && echo "AUTHORITY ENFORCEMENT VERIFIED"
```
</verification>

<success_criteria>
- `anchor test` exits 0
- All tests pass (0 failing)
- "rejects update from unauthorized signer" test passes — proves no authority bypass
- "transfers authority to a new key" test passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-anchor-program/01-02-SUMMARY.md`
</output>
