# Phase 1: Anchor Program - Context

**Gathered:** 2026-04-10
**Status:** Ready for planning

<domain>
## Phase Boundary

Build, test, and deploy the Delos Oracle Anchor program to Solana devnet. The program source is already written — this phase completes the build pipeline (anchor build → anchor test → anchor deploy devnet) and documents the Program ID.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices are at Claude's discretion — pure infrastructure phase. Program source (`programs/delos-oracle/src/lib.rs`) and tests (`tests/anchor/delos-oracle.ts`) are already written. Phase focuses on build, test, deploy, and documentation.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `programs/delos-oracle/src/lib.rs` — fully implemented: `MacroOracle` account, all 5 instructions, `Rate` and `FocusRate` structs
- `tests/anchor/delos-oracle.ts` — fully written: initialize, update_rate, batch_update, focus, authority enforcement, transfer_authority tests
- `Anchor.toml` — configured for devnet (`cluster = "devnet"`) with keypair at `target/deploy/delos_oracle-keypair.json`
- `target/deploy/delos_oracle-keypair.json` — keypair exists (program identity established)

### Established Patterns
- PDA seeds: `[b"oracle"]`
- Rate scaling: ×10^8 (Chainlink-compatible)
- Date format: YYYYMMDD as `u32`
- Authority enforcement via `has_one = authority @ DelosError::Unauthorized`

### Integration Points
- `solana-crank/src/publish.ts` loads IDL from `target/idl/delos_oracle.json` — IDL must be generated by `anchor build`
- Program ID `6VsTr6k9EkNHhjDVjNn5yfbvjAqjozgSDDCstT1k2frc` already in `Anchor.toml` (devnet)

</code_context>

<specifics>
## Specific Ideas

No specific requirements — infrastructure phase. Just need to run the build pipeline and confirm tests pass.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>
