---
phase: 01-anchor-program
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - target/idl/delos_oracle.json
  - target/types/delos_oracle.ts
autonomous: true
requirements:
  - F1.1
  - F1.2
  - F1.3
  - F1.4
  - F2.1
  - F2.2
  - F2.3
  - F3.1
  - F3.2
  - F3.3
  - F3.4
  - NF3.1

must_haves:
  truths:
    - "anchor build exits 0 with no compilation errors"
    - "target/idl/delos_oracle.json exists and contains all 5 instructions"
    - "target/types/delos_oracle.ts exists and exports the DelosOracle type"
    - "target/deploy/delos_oracle.so exists (BPF binary)"
  artifacts:
    - path: "target/idl/delos_oracle.json"
      provides: "On-chain interface definition used by tests and publisher"
      contains: "initialize"
    - path: "target/types/delos_oracle.ts"
      provides: "TypeScript types for Anchor client"
      exports: ["DelosOracle"]
    - path: "target/deploy/delos_oracle.so"
      provides: "Compiled BPF program binary for deployment"
  key_links:
    - from: "programs/delos-oracle/src/lib.rs"
      to: "target/idl/delos_oracle.json"
      via: "anchor build IDL generation"
      pattern: "anchor build"
    - from: "target/idl/delos_oracle.json"
      to: "solana-crank/src/publish.ts"
      via: "IDL import at runtime"
      pattern: "require.*target/idl"
---

<objective>
Run `anchor build` to compile the Delos Oracle Anchor program and generate the IDL and TypeScript type definitions that all downstream tasks require.

Purpose: The program source is already complete. This plan produces the build artifacts — IDL, TypeScript types, and BPF binary — that the test suite and publisher depend on. Nothing else can run until this succeeds.

Output: `target/idl/delos_oracle.json`, `target/types/delos_oracle.ts`, `target/deploy/delos_oracle.so`
</objective>

<execution_context>
@/Users/pedrotodescan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedrotodescan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-anchor-program/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run anchor build and verify artifacts</name>
  <read_first>
    - programs/delos-oracle/src/lib.rs (source being compiled)
    - programs/delos-oracle/Cargo.toml (anchor-lang = "1.0.0" — verify Anchor CLI version matches)
    - Anchor.toml (workspace config, program address)
    - Cargo.toml (workspace members)
  </read_first>
  <files>
    target/idl/delos_oracle.json
    target/types/delos_oracle.ts
    target/deploy/delos_oracle.so
  </files>
  <action>
    Run the build command from the project root:

    ```
    anchor build
    ```

    Expected: Rust compilation completes, Anchor generates IDL and TypeScript types.

    If the build fails:
    - Check Rust/Anchor toolchain versions: `anchor --version`, `rustc --version`, `solana --version`
    - Anchor 0.30.x requires anchor-lang = "0.30.x" in Cargo.toml. The program uses anchor-lang = "1.0.0" — if toolchain mismatch causes failure, check `~/.cargo/bin/anchor --version` and align.
    - For BPF target errors: ensure `rustup target add bpfel-unknown-unknown` is installed
    - For `anchor-lang` version conflict: verify the installed Anchor CLI matches the Cargo.toml version exactly

    Do NOT modify lib.rs or any program source files. Build failures are toolchain issues, not code issues.

    After successful build, confirm all three artifacts exist:
    - `target/idl/delos_oracle.json` — must contain keys: "instructions", "accounts", "types", "errors"
    - `target/types/delos_oracle.ts` — must export `DelosOracle` interface
    - `target/deploy/delos_oracle.so` — BPF binary
  </action>
  <verify>
    <automated>anchor build && test -f target/idl/delos_oracle.json && test -f target/types/delos_oracle.ts && test -f target/deploy/delos_oracle.so && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `anchor build` exits with code 0
    - `target/idl/delos_oracle.json` exists and is valid JSON: `python3 -m json.tool target/idl/delos_oracle.json > /dev/null` succeeds
    - IDL contains all 5 instructions: grep for "initialize", "updateRate", "batchUpdateRates", "updateFocus", "transferAuthority" in target/idl/delos_oracle.json
    - `target/types/delos_oracle.ts` exists and contains "DelosOracle": grep "DelosOracle" target/types/delos_oracle.ts returns matches
    - `target/deploy/delos_oracle.so` exists: `test -f target/deploy/delos_oracle.so` exits 0
  </acceptance_criteria>
  <done>All three build artifacts exist. anchor build exited 0. IDL contains all 5 instruction names. TypeScript types export DelosOracle.</done>
</task>

</tasks>

<verification>
```
anchor build && \
  python3 -m json.tool target/idl/delos_oracle.json > /dev/null && \
  grep -q "initialize" target/idl/delos_oracle.json && \
  grep -q "updateRate" target/idl/delos_oracle.json && \
  grep -q "batchUpdateRates" target/idl/delos_oracle.json && \
  grep -q "updateFocus" target/idl/delos_oracle.json && \
  grep -q "transferAuthority" target/idl/delos_oracle.json && \
  grep -q "DelosOracle" target/types/delos_oracle.ts && \
  test -f target/deploy/delos_oracle.so && \
  echo "ALL ARTIFACTS VERIFIED"
```
</verification>

<success_criteria>
- `anchor build` exits 0
- `target/idl/delos_oracle.json` is valid JSON containing all 5 instruction names
- `target/types/delos_oracle.ts` exports `DelosOracle`
- `target/deploy/delos_oracle.so` exists (BPF binary ready for deployment)
</success_criteria>

<output>
After completion, create `.planning/phases/01-anchor-program/01-01-SUMMARY.md`
</output>
